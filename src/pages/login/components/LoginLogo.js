import React from 'react';

import { Logo } from '../../molecules';

const LoginLogo = props => {
    const { } = props;

    return (
        <div>
            <Logo />
            <div></div>
        </div>
    );
};

export { LoginLogo };
